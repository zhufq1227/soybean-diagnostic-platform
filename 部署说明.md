# 豆壤智诊 · 单文件离线版

## 本地使用

直接双击 `index.html`。土样、ASV和知识库数据只保存在浏览器当前页面内存中；刷新或关闭页面会清空，请提前导出Excel或完整备份JSON。

## GitHub Pages

1. 新建GitHub仓库，将 `index.html` 上传到仓库根目录。
2. 打开 Settings → Pages。
3. Source选择 Deploy from a branch；Branch选择 main 与 /(root)。
4. 保存后等待部署完成。

也可把同一个 `index.html` 上传到Cloudflare Pages、Netlify、Vercel或普通静态服务器，无需构建命令。

## 隐私与边界

- 文件解析和运算均在浏览器本地完成，没有网络请求。
- 程序内置680篇已整理文献、文献派生菌属记录、42个官方审定品种和20类土壤映射；用户上传数据不会写入HTML。
- 推荐为科研参考，不构成商业种植处方；必须经当地农艺师复核和小范围田间试验。

## 后续接口

气象API、后端加密存储、模板化PDF、多样性/差异丰度分析的扩展边界已写在平台“导出与部署”页面。
